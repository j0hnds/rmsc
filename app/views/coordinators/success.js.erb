$('#record_manager')
        .fadeOut('fast', function() {
            $(this).empty();
        })
        .pulse({
            callback : function() {
                $('#coordinators_list')
                    .css('display', 'none')
                    .html("<%= escape_javascript(render(:partial => 'list')) -%>")
                    .fadeIn('fast')
            }
});