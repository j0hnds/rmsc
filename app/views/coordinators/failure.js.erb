/* Scroll to top of page */
$('html, body').animate({scrollTop:0}, 'normal');

/* Display error message */
$('#module_messages')
        .html('<%= render('/shared/error_messages', :target => @coordinator).to_s.gsub("'","\'") -%>')
        .pulse();